version: '3'

services:
  client:
    build: ./client
    tty: true
    # command: npm run dev
    volumes:
      - ./client:/app
    ports:
      - "3000:3000"
  server:
    build: ./server
    tty: true
    command: go run main.go
    volumes:
      - ./server:/app
    ports:
      - "8080:8080"
